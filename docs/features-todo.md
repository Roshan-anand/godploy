# Features TODO — Future Polish

These are deferred feature ideas that came up during the settings/polish discussion.
Not part of extras-v1. May be revisited later.

## Deferred

- Promote a preview instance to production — tested preview becomes new stable runtime without rebuild from scratch
- Seeded preview data / snapshot-based database init — realistic test data instead of empty stateful services
- Rich preview topology editing — add/remove/clone services inside a preview without touching production
- [ ] **Session management** — view active sessions and revoke them
- [ ] **Project archive/suspend** — archive a project without deleting it
- [ ] **Auto-deploy toggle per service** — disable auto-deploy on push for a specific service
- [ ] **Resource limits per service** — CPU/memory constraints configurable in service settings
- [ ] **Max concurrent previews** — limit per project or per org
- [ ] **Webhook status visibility** — show webhook health, last delivery, re-trigger in dashboard
- [ ] **Notification preferences** — email notifications for deploy failures, toast settings
