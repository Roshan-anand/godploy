# Pull Build Deploy

## Overview
The crucial part of a deployment platform is to pull the code, build it and deploy it. th eproject pulls code using git, build the OCI image using dockerfile, nixpack etc and deploys it using docker stack inside swarm.


