## setup script building guide

- The script must be run as root. use `$(id -u)` if output is `0`, then you are root.
- installed required packahes :
  - [ ] Docker
  - [ ] git
  
- enable docker service and start it